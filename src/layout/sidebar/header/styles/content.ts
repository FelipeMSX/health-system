import styled from "styled-components";

export const Content = styled.div`
  display: flex;
  align-items: center;

  > span {
    font-size: 16px;
    color: var(--white);
    font-weight: 500;
    text-transform: uppercase;
    margin-left: 80px;
  }

  > img {
    width: 24px;
    height: 24px;
  }
`;
