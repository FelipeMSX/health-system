import styled from "styled-components";

export const Title = styled.div`
  display: flex;
  margin-left: 12px;
  margin-top: 40px;
  color: var(--white);
  width: 100%;
  height: 40px;
  align-items: center;

  > h1 {
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    opacity: 0.7;
  }
`;
