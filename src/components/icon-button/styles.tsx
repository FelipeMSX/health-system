import { Props } from ".";
import styled from "styled-components";
import { IconButton } from "@mui/material";

export const StyledButton = styled(IconButton)<Props>`
  display: flex;
  width: 40px;
  height: 40px;
`;
